# 🐛 Step 1: Fix the Calculation

A bug has been reported in the order calculation.

Given:

- Price: `100`
- Quantity: `3`

The application currently returns:

`103`

The expected result is:

`300`

## Your task

1. Create a new branch named `fix/calculation-bug`.
2. Open `src/calculator.js`.
3. Investigate the `calculateTotal()` function.
4. Fix the calculation.
5. Commit your change.
6. Open a Pull Request targeting `main`.

> Do not merge your Pull Request yet.

GitHub will automatically check your solution.
